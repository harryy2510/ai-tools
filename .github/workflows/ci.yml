name: ci

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  check:
    name: check
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version-file: package.json

      - name: Cache Bun install
        uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - name: Install
        run: bun install --frozen-lockfile

      - name: Format check
        run: bun run format:check

      - name: Lint (type-aware)
        run: bun run lint

      - name: Codegen drift check
        run: bun run codegen:check

      - name: Test
        run: bun run test

      - name: Typecheck
        run: bun run typecheck

      - name: Build
        run: bun run build

      - name: Pack dry-run
        run: bun pm pack --dry-run

      - name: Assert package files
        run: |
          set -euo pipefail
          # Brain
          test -f dist/core/index.js
          test -f dist/core/index.d.ts
          test -f dist/http/index.js
          # Seams (modules/)
          test -f dist/modules/storage/index.js
          test -f dist/modules/email/index.js
          test -f dist/modules/messaging/index.js
          test -f dist/modules/email-message/index.js
          # Vendors (vendors/)
          test -f dist/vendors/resend/index.js
          test -f dist/vendors/cloudflare-email/index.js
          test -f dist/vendors/s3/index.js
          test -f dist/vendors/telegram/index.js
          test -f dist/vendors/slack/index.js
          test -f dist/vendors/teams/index.js
